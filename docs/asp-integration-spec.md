# Superseded AutoSEOPilot handoff

This 2026-07-04 handoff is superseded. The canonical V1 contract is:

`docs/autoseopilot-managed-integration.md`

That contract replaces the earlier email-keyed `/internal/provision` and
`/internal/revoke` proposal with workspace-UUID-keyed managed hosting,
durable credential generations, explicit entitlement gates, and
`/internal/autoseopilot` routes.
