# Implementation Plans

Generated by the improve skill on 2026-06-10 against commit `c95b816`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| 001 | Add a launch smoke suite that covers the real contract | P1 | M | - | DONE |
| 002 | Make default public blog URLs real in hosted and self-host deployments | P1 | M | 001 recommended | DONE |
| 003 | Add CSRF protection to session-authenticated mutations | P1 | M | 001 recommended | DONE |
| 004 | Make post mutations atomic with versions and activity | P1 | M | 001 recommended | DONE |
| 005 | Harden MCP and REST contracts for agent-first launch | P1 | M | 001 recommended | DONE |
| 006 | Add workspace API and MCP quotas | P1 | M | 001 and 005 recommended | DONE |
| 007 | Resolve SSR Error 1102 (Workers Paid) and wire public blog subdomains | P0 | S | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

Post-006 launch hardening (verified 2026-06-13): API tokens capped per workspace (leak guard) and the yearly checkout product fallback fixed so yearly never silently charges the monthly product. See CHANGELOG.

## Dependency notes

- 001 should land first because it gives the rest of the launch fixes a behavioral tripwire: signup/setup, UI draft creation, token creation, MCP create/publish, REST read, and export leak checks.
- 002, 003, 004, and 005 can run after 001 in parallel if different executors coordinate, but each must extend or preserve the smoke suite.
- 006 should land after or alongside 005 because it depends on the final MCP/REST contract shape and should share the same smoke coverage.
- 004 and 005 both touch MCP/post mutation behavior; if done in parallel, merge 004 first, then rebase 005 onto the atomic repository contract.

## Audit summary

Verification observed during this audit:

- `pnpm typecheck` passed.
- `pnpm lint` passed.
- `pnpm build` passed.
- `pnpm public:audit` passed, with the expected warning that local env files exist and must remain untracked.
- `pnpm audit --audit-level high` failed on `tmp <0.2.6` through `apps__web > rwsdk > tmp-promise > tmp` (GHSA-ph9p-34f9-6g65).
- Local smoke succeeded for signup, setup, dashboard, UI draft creation, token creation, MCP `initialize`, MCP `tools/list`, MCP `posts.create`, MCP `posts.publish`, REST `GET /api/posts`, and JSON export with no token leak.
- Remote deployed smoke against `https://vibecms.moinulislammoin2019.workers.dev` showed the deployment is stale relative to `main`: old homepage copy, old metadata, `/brand/og-image.png` 404. Signup/setup worked and correctly redirected to `/app/billing` in hosted mode.

## Findings considered and rejected or deferred

- **Auth form has no no-JS fallback**: real issue (`apps/web/src/app/pages/auth-form.tsx` is client-only), but lower priority than CSRF/public URL/MCP contract for first public alpha. Revisit after P1 plans.
- **Public post-card hover selector is wrong**: real small UI bug (`.postCard a:hover h2` does not match current DOM). Fix opportunistically; not worth a full plan.
- **Token reveal wrapping/mobile polish**: useful UX polish, but not launch-blocking after copy button exists.
- **Dashboard export discoverability**: add owner-only Export to quick actions when touching dashboard next.
- **Public media route serves all uploaded assets by ID**: important product/security decision. If draft media must be private, write a dedicated P1 plan before public launch; if assets are intentionally public-on-upload, document that explicitly.
- **SEO fields accepted but not persisted**: validators/schema/export/public rendering mention SEO/canonical fields but core commands do not write them. Decide whether to remove for minimal launch or make first-class before exposing SEO UI/API.
- **Scheduled status remains in initial SQL**: cleanup needed; can be folded into a migration hygiene pass unless existing D1 has scheduled rows.
- **Yearly pricing can fall back to monthly product**: before paid hosted launch, either require `POLAR_YEARLY_PRODUCT_ID` when yearly is advertised or hide yearly UI if unset.
- **Self-host deploy button unrehearsed**: docs now frame self-hosting honestly for Cloudflare-comfortable users. Still rehearse the clean-account deploy before promoting one-click deploy.
- **Dependency audit high finding in transitive `tmp`**: track upstream `rwsdk`/`tmp-promise` update or override if exploitability reaches runtime. Not planned here because it is transitive and may be dev/build-path only; verify before public release.

## Direction suggestions

- Productize the static sidebar “Launch Path” into a real checklist driven by dashboard data: first post, cover image, token connected, public URL live.
- Add MCP connection health in Settings using token `lastUsedAt` and recent activity, so users know when an agent actually connected.
- Add a lightweight import/restore spike only after export stabilizes; keep the first import posts-only and draft-only.
- Add theme previews in Settings using the existing public theme CSS so users choose presets with real content rather than labels only.
